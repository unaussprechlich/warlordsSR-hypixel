extends layout

mixin td_item(item)
  if item
    td= item
  else
    td= ""

block content
  #content
    .table-responsive
      table.table.table-striped.table-hover#sr_table
        thead
          block table

block js
  script
    include tablesorter.js